// Includes certos

// Função principal
User Function PRINCIPAL()
    CriaPergunte() // Cria as perguntas com a classe top JBIG
    
    // MsgYesNo() + Pergunte(.T.) para o usuário informar se quer prosseguir
    If (ExibeParametros())
        CriaTabela() // Cria a tabela bonitinha com Embedded SQL
        CriaXML() // Gera XML com FwMsExcel() ou dependendo do tamanho do arquivo FwMsExcelEx()
        AbreExcel() // Abre o Excel com a MsExecel()
    EndIf
Return (NIL)

